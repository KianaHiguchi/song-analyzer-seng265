# Assignment 2 – Song Analyzer (Python)

Python implementation of a command-line tool that reads a CSV file of songs, supports filtering + sorting based on CLI arguments, and writes results to output.csv.

```bash
python3 song_analyzer.py --data=data.csv --filter=ARTIST --value="Dua Lipa" --order_by=STREAMS --order=ASC --limit=6

The program writes its results to output.csv.
